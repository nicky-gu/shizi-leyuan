# 识字网站发音核查与改进方案

## 一、拼音数据严格核查
**方法**：用 pypinyin 对每个字取全部合法带调读音，与 `data.js` 的 `p` 字段逐一比对。
**结论**：607 字中 **0 个硬错误**（所有标注都是该字的合法读音）。

**多音字**：262 个（占 43%）。其中约 12 个标了"非默认"读音，大部分是教材固定读法（正确），少数需对照课文确认语境。

## 二、pypinyin 的局限（重要）
pypinyin 只能判断"读音是否合法"，**不能判断"课文语境是否正确"**。
例：「尽」在古诗二首"白日依山尽"应读 **jìn**，但 `data.js` 标的是 **jǐn**（也是合法音，脚本查不出）。这类"合法但语境错"需要人工对照课本。

**已发现疑似语境错误**：
| 字 | data.js 标注 | 应为 | 出处 |
|---|---|---|---|
| 尽 | jǐn | **jìn** | 古诗二首·登鹳雀楼"白日依山尽" |

## 三、需对照教材确认语境的多音字（非默认读音）
| 字 | 标注 | 合法音 | 我的初步判断 |
|---|---|---|---|
| 行 | háng | xíng/háng… | ✓ 场景歌"一行垂柳"读 háng，正确 |
| 数 | shǔ | shù/shǔ… | ✓ "数星星的孩子"动词读 shǔ，正确 |
| 干 | gān | gàn/gān | ✓ 葡萄沟"葡萄干"读 gān，正确 |
| 得 | de | dé/de/děi | ✓ 植物妈妈"得"轻声音，正确 |
| 担 | dàn | dān/dàn | ✓ 朱德的扁担，正确 |
| 转 | zhuàn | zhuǎn/zhuàn | ⚠ 语文园地一，需确认（转盘/转身） |
| 当 | dàng | dāng/dàng | ⚠ 需确认课文语境 |
| 重 | chóng | zhòng/chóng | ⚠ 需确认（重新/重要） |
| 笼 | lǒng | lóng/lǒng | ⚠ 可能在"笼罩"，需确认 |
| 杆 | gǎn | gān/gǎn | ⚠ 需确认课文语境 |
| 扎 | zā | zhā/zā/zhá | ⚠ 可能在"扎风筝"zā，需确认 |
| 朝 | zhāo | cháo/zhāo | ⚠ 需确认课文语境 |

> 以上"⚠"项建议对照孩子课本逐字确认；确认后我改 `data.js`。

## 四、发音"不准 + 跨端不一致"的真正根因
当前 `app.js` 的 `speak()`：
```js
speechSynthesis.cancel();
const u = new SpeechSynthesisUtterance(text); // text 是"汉字"
u.lang = "zh-CN"; u.rate = 0.7;
speechSynthesis.speak(u);
```
三点问题：
1. **传汉字而非拼音** → 浏览器 TTS 自己判断读音，多音字按默认音读（如"行"读 xíng 而非教材 háng），和学的对不上。
2. **没指定 voice** → 各终端用系统自带引擎（Win=Huihui/Yaoyao，mac=婷婷，安卓/iOS 各异），所以音色、腔调都不一样。
3. **rate=0.7 慢速** → 部分引擎在慢速下变调，声调听着怪。

所以"不同终端声音不同"和"有些字听着不对 / 声调怪"，主要不是拼音数据错，而是这个发音机制。

## 五、治本方案（已验证可行）
**思路**：不靠浏览器临时念，预先用高质量 TTS 按 `data.js` 的**正确拼音**生成每个字的发音音频，网站直接播放固定文件。跨端 100% 一致，读音 100% 准确。

**验证**：已用 edge-tts + SSML `<phoneme>` 强制按指定声调生成样本：
- `行` → 强制读 **háng** ✅
- `得` → 强制读 **de**（轻声）✅
（样本见附带的 `audio_test/hang.mp3` / `audio_test/de.mp3`，可直接试听音质与读音）

**优点**：
- 跨端一致：所有设备播同一份音频
- 音质高：微软神经网络中文语音
- 多音字绝对准：按标注拼音生成，不受 TTS 自判影响
- 声调准：慢速变调问题消失

**体积优化**：原始单字 ~130KB，607 字约 80MB 偏大。会用 ffmpeg 压到 ~24kbps 单声道（单字约 8–12KB，总约 6MB），按需懒加载（点哪个播哪个，不一次性下载）。

**实施步骤**：
1. 修正上面确认错/需改的拼音（尽→jìn 等）
2. 批量生成 607 个 mp3 到 `assets/audio/`（含 ffmpeg 压缩）
3. 改 `app.js` 的 `speak()`：优先播放对应音频文件，失败再 fallback 浏览器 TTS
4. 重新部署到 Pages

## 六、下一步（请你定）
- **A（推荐，治本）**：我直接做全量音频方案 + 修正拼音，重新部署。
- **B（次优，快）**：先只修正拼音数据（尽等）+ 在 `speak()` 里锁定一个高质量中文 voice、把 rate 调到 0.85；不保证跨端完全一致，但立刻改善。
- 另外：第三节"⚠"的多音字，需要你对照孩子课本确认语境（或授权我按二上教材常识定）。

附：发音核查脚本 `check_pinyin.py`、音频生成验证 `test_tts.py` 已留在 `shizi/`。
